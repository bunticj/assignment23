export enum GameStateType {
    GameWaiting = 1,
    GameStarting = 2,
    GameInProgress = 3,
    GameIntermission = 4, //state between round change
    GameFinished = 5,
    GameCancelled = 6,
   
}